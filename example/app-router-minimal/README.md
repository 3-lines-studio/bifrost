# Minimal App Router app

This app only needs `page.tsx`. It has no user Go files and does not need its own `go.mod`.

```sh
go run ./cmd/bifrost dev ./example/app-router-minimal
go run ./cmd/bifrost build ./example/app-router-minimal
./example/app-router-minimal/.bifrost/bifrost-app
```
