import React from "react";
import { createRoot } from "react-dom/client";
import { Page } from "COMPONENT_PATH";

const container = document.getElementById("app");
if (container) {
	const root = createRoot(container);
	root.render(BIFROST_CLIENT_ROOT);
}
