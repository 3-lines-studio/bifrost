import { mount } from "svelte";
import Page from "COMPONENT_PATH";
const target = document.getElementById("app");
if (target) {
	target.innerHTML = "";
	mount(BIFROST_CLIENT_ROOT, { target, props: {} });
}
